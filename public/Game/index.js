class Bomb {
  constructor(id, name, radius, diagRadius, timer, cooldown) {
    this.id = id;
    this.name = name;
    this.radius = radius;
    this.diagRadius = diagRadius;
    this.timer = timer;
    this.cooldown = cooldown;
  }
}
